"""UniMem event-memory training / serving / deployment for the bimanual robots.

See README.md in this directory for the full walkthrough. Nothing here modifies the
openpi source tree; the configs register themselves into ``openpi.training.config``
when :func:`custom_unimem._bootstrap.bootstrap` runs.
"""
